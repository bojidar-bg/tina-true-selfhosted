export function MDXEditorField() {
  return <>
    This dummy module should not be loaded by browser--it exists mainly to workaround issues in Tina's build system which don't allow root-level await used by MDXEditor's dependencies.
  </>;
}

export default MDXEditorField
